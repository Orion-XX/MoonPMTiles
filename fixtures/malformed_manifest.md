# Malformed corpus manifest

These in-source byte corpora are deterministic, offline, and intentionally
minimal. They exercise truncated headers, invalid enum/version bytes, overlong
varints, and invalid tile coordinates. No external fixture or network source is
used.
